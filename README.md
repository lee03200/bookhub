# 📚 BookHub - 在线图书阅读平台（前端）

> 基于 Vue 3 + Vite + Tailwind CSS 的现代化图书推荐与管理系统前端应用

[![Vue](https://img.shields.io/badge/Vue-3.4.21-brightgreen.svg)](https://vuejs.org/)
[![Vite](https://img.shields.io/badge/Vite-5.2.8-646CFF.svg)](https://vitejs.dev/)
[![TailwindCSS](https://img.shields.io/badge/TailwindCSS-3.4.3-38B2AC.svg)](https://tailwindcss.com/)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

---

## 📖 项目简介

BookHub 是一个功能完善的在线图书阅读平台前端应用，提供书籍推荐、个人书架管理、评论互动等功能。采用最新的前端技术栈，提供流畅的用户体验和优雅的界面设计。

### ✨ 核心特性

- 🔐 **完整的用户认证系统** - 注册、登录、个人资料管理
- 📚 **智能书籍推荐** - 基于用户兴趣的个性化推荐
- 🔥 **热门书籍榜单** - 实时更新的热门图书排行
- 📖 **个人书架管理** - 收藏、分类、阅读进度跟踪
- 💬 **评论互动系统** - 发表评论、查看书评
- 📱 **响应式设计** - 完美适配移动端和桌面端
- 🎨 **现代化 UI** - 基于 Tailwind CSS 的精美界面
- ⚡ **极速加载** - Vite 驱动的快速开发和构建

---

## 🛠️ 技术栈

### 核心框架
- **Vue 3.4.21** - 渐进式 JavaScript 框架
- **Vite 5.2.8** - 下一代前端构建工具
- **Vue Router 4.3.0** - 官方路由管理器
- **Pinia 2.1.7** - Vue 官方状态管理库

### UI 与样式
- **Tailwind CSS 3.4.3** - 实用优先的 CSS 框架
- **PostCSS 8.4.38** - CSS 转换工具
- **Autoprefixer 10.4.19** - 自动添加浏览器前缀

### HTTP 客户端
- **Axios 1.6.8** - Promise 基础的 HTTP 库

---

## 📁 项目结构

```
bookhub/
├── src/
│   ├── api/                    # API 接口封装
│   │   └── index.js           # Axios 实例和接口定义
│   ├── assets/                 # 静态资源
│   │   └── styles/            # 全局样式
│   ├── components/             # 公共组件
│   │   ├── BookCard.vue       # 书籍卡片组件
│   │   ├── NavBar.vue         # 导航栏组件
│   │   ├── StarRating.vue     # 星级评分组件
│   │   ├── PopularBookItem.vue    # 热门书籍项
│   │   ├── ReadingBookItem.vue    # 阅读书籍项
│   │   └── VipPlanCard.vue    # VIP 计划卡片
│   ├── data/                   # 静态数据
│   │   └── books.js           # 书籍数据
│   ├── router/                 # 路由配置
│   │   └── index.js           # 路由定义
│   ├── stores/                 # Pinia 状态管理
│   │   ├── index.js           # Store 入口
│   │   ├── user.js            # 用户状态
│   │   └── books.js           # 书籍状态
│   ├── views/                  # 页面组件
│   │   ├── home.vue           # 首页
│   │   ├── ranking.vue        # 热门书籍榜单
│   │   ├── profile.vue        # 个人中心
│   │   ├── RecommendedBooks.vue   # 推荐书籍页
│   │   ├── BookDetail.vue     # 书籍详情
│   │   └── Category.vue       # 分类页面
│   ├── App.vue                # 根组件
│   ├── main.js                # 应用入口
│   └── index.css              # 全局样式
├── .env                        # 环境变量配置
├── index.html                  # HTML 模板
├── package.json                # 依赖配置
├── vite.config.js             # Vite 配置
├── tailwind.config.js         # Tailwind 配置
└── README.md                   # 项目说明
```

---

## 🚀 快速开始

### 前置要求

- **Node.js** >= 16.0.0
- **npm** >= 8.0.0 或 **yarn** >= 1.22.0
- **后端 API 服务**（位于 `../bookhubbacked`）

### 安装步骤

#### 1. 克隆项目

```bash
cd bookhub/bookhub
```

#### 2. 安装依赖

```bash
# 使用 npm
npm install

# 或使用 yarn
yarn install
```

#### 3. 配置环境变量

编辑 `.env` 文件，配置后端 API 地址：

```env
# API 后端地址
VITE_API_URL=http://localhost:8000/api
```

#### 4. 启动开发服务器

```bash
# 使用 npm
npm run dev

# 或使用 yarn
yarn dev
```

应用将运行在 `http://localhost:5173`

#### 5. 构建生产版本

```bash
# 使用 npm
npm run build

# 或使用 yarn
yarn build
```

#### 6. 预览生产构建

```bash
# 使用 npm
npm run preview

# 或使用 yarn
yarn preview
```

---

## 🎯 主要功能

### 1. 用户认证

- ✅ 用户注册（用户名、密码）
- ✅ 用户登录（Token 认证）
- ✅ 自动登录（Token 持久化）
- ✅ 退出登录
- ✅ 个人资料编辑

### 2. 首页

- ✅ 欢迎引导页（未登录）
- ✅ 为你推荐（智能推荐）
- ✅ 热门书籍（榜单预览）
- ✅ 浏览分类（分类筛选）
- ✅ 我的书架（快速访问）
- ✅ 阅读足迹（统计数据）

### 3. 推荐书籍

- ✅ 所有推荐书籍展示
- ✅ 分类筛选（全部/小说/科技/历史等）
- ✅ 排序功能（默认/评分/评价数）
- ✅ 分页显示（每页 20 本）
- ✅ 统计信息（总数/已收藏）

### 4. 热门书籍

- ✅ 热门榜单展示
- ✅ 分类筛选
- ✅ 书籍详情查看
- ✅ 评分与评论
- ✅ 添加到书架

### 5. 个人中心

- ✅ 个人资料展示
- ✅ 头像上传
- ✅ 资料编辑（用户名/简介/性别/兴趣）
- ✅ 阅读统计（总阅读数/收藏数/平均评分）
- ✅ 我的书架管理
- ✅ 最近在读
- ✅ 我的评论

### 6. 书籍详情

- ✅ 书籍信息展示
- ✅ 评分系统
- ✅ 评论功能
- ✅ 收藏/取消收藏
- ✅ 阅读进度跟踪

---

## 🔌 API 集成

### API 配置

位置：`src/api/index.js`

```javascript
// API 基础地址
const API_BASE = import.meta.env.VITE_API_URL || 'http://localhost:8000/api'

// 请求拦截器 - 自动添加 Token
instance.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Token ${token}`
  }
  return config
})
```

### 主要接口

#### 用户相关
- `POST /users/register/` - 用户注册
- `POST /users/login/` - 用户登录
- `POST /users/logout/` - 用户登出
- `GET /users/profile/` - 获取用户资料
- `PUT /users/profile/` - 更新用户资料

#### 书籍相关
- `GET /books/` - 获取书籍列表
- `GET /books/{id}/` - 获取书籍详情
- `GET /books/recommended/` - 获取推荐书籍
- `GET /books/popular/` - 获取热门书籍

#### 书架相关
- `GET /bookshelf/` - 获取我的书架
- `POST /bookshelf/` - 添加书籍到书架
- `DELETE /bookshelf/{id}/` - 从书架移除

#### 评论相关
- `GET /comments/` - 获取评论列表
- `POST /comments/` - 发表评论
- `GET /comments/my/` - 获取我的评论

---

## 📊 状态管理

### User Store（用户状态）

位置：`src/stores/user.js`

```javascript
// 状态
state: {
  token: '',          // 认证令牌
  user: null,         // 用户信息
  profile: {},        // 用户资料
  info: {}            // 附加信息
}

// 操作
actions: {
  init()              // 初始化检查
  register()          // 用户注册
  login()             // 用户登录
  logout()            // 用户登出
  loadUserProfile()   // 加载资料
  updateProfile()     // 更新资料
}
```

### Books Store（书籍状态）

位置：`src/stores/books.js`

```javascript
// 状态
state: {
  allBooks: [],       // 所有书籍
  myShelfBooks: [],   // 我的书架
  currentlyReading: [],   // 正在阅读
  myComments: [],     // 我的评论
  popular: [],        // 热门书籍
  readingStats: {}    // 阅读统计
}

// 操作
actions: {
  toggleFavorite()    // 切换收藏
  addToShelf()        // 添加到书架
  removeFromShelf()   // 从书架移除
}
```

---

## 🎨 样式系统

### Tailwind CSS 配置

位置：`tailwind.config.js`

主要使用的工具类：

```css
/* 布局 */
.container, .max-w-7xl, .mx-auto, .px-4

/* 网格 */
.grid, .grid-cols-2, .sm:grid-cols-3, .md:grid-cols-4, .lg:grid-cols-5

/* Flexbox */
.flex, .flex-col, .items-center, .justify-between

/* 颜色 */
.bg-indigo-600, .text-white, .text-gray-700

/* 间距 */
.p-4, .py-8, .mb-4, .gap-6

/* 圆角与阴影 */
.rounded-lg, .shadow-md, .shadow-lg
```

---

## 📱 响应式设计

### 断点

- **sm**: >= 640px（小屏幕）
- **md**: >= 768px（中等屏幕）
- **lg**: >= 1024px（大屏幕）
- **xl**: >= 1280px（超大屏幕）

### 网格布局示例

```vue
<!-- 手机2列，平板3列，桌面4列，大桌面5列 -->
<div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-6">
  <!-- 书籍卡片 -->
</div>
```

---

## 🔒 权限控制

### 路由守卫

位置：`src/router/index.js`

```javascript
router.beforeEach((to, from, next) => {
  const isLoggedIn = localStorage.getItem('isLoggedIn') === 'true'
  if (to.path === '/profile' && !isLoggedIn) {
    next()  // 由组件内部显示登录页面
  } else {
    next()
  }
})
```

### 页面级权限

- **首页**: 未登录显示引导页，已登录显示完整内容
- **热门书籍**: 未登录显示登录提示
- **推荐书籍**: 未登录显示登录提示
- **个人中心**: 未登录显示登录提示

---

## 🧪 开发指南

### 添加新页面

1. 在 `src/views/` 创建 Vue 组件
2. 在 `src/router/index.js` 添加路由
3. 在导航栏添加链接

### 添加新组件

1. 在 `src/components/` 创建 Vue 组件
2. 在需要的页面中导入使用

### 添加新 API

1. 在 `src/api/index.js` 定义接口方法
2. 在组件中调用

### 状态管理最佳实践

```javascript
// 1. 定义 store
export const useMyStore = defineStore('my', {
  state: () => ({ data: [] }),
  actions: {
    async fetchData() {
      // API 调用
    }
  }
})

// 2. 在组件中使用
import { useMyStore } from '@/stores/my'
const myStore = useMyStore()
```

---

## 📝 开发规范

### 代码风格

- 使用 ES6+ 语法
- 组件使用 `<script setup>` 语法
- 使用 Composition API
- 遵循 Vue 3 官方风格指南

### 命名规范

- **组件**: PascalCase（如 `BookCard.vue`）
- **文件**: camelCase（如 `userStore.js`）
- **变量**: camelCase（如 `isLoggedIn`）
- **常量**: UPPER_CASE（如 `API_BASE`）

### 注释规范

```javascript
// 单行注释

/**
 * 多行注释
 * @param {Object} data - 数据对象
 * @returns {Promise} - 返回 Promise
 */
```

---

## 🐛 常见问题

### 1. API 请求失败

**问题**: 请求返回 404 或连接被拒绝

**解决**:
- 检查后端服务是否启动（`http://localhost:8000`）
- 检查 `.env` 文件中的 `VITE_API_URL` 配置
- 检查网络代理设置

### 2. Token 认证失败

**问题**: 登录后仍显示未登录

**解决**:
- 清除浏览器缓存和 localStorage
- 检查 Token 格式（应为 `Token xxx`）
- 重新登录获取新 Token

### 3. 样式不生效

**问题**: Tailwind CSS 样式未应用

**解决**:
- 重启开发服务器
- 检查 `tailwind.config.js` 配置
- 确保已导入 `index.css`

### 4. 路由跳转失败

**问题**: 点击链接无反应

**解决**:
- 检查路由配置是否正确
- 使用 `<router-link>` 而非 `<a>`
- 检查路由路径拼写

### 5. 页面刷新后状态丢失

**问题**: 刷新后登录状态消失

**解决**:
- 检查 localStorage 是否正常存储
- 在 `App.vue` 的 `onMounted` 中调用 `userStore.init()`

---

## 🔧 配置文件说明

### `vite.config.js`

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': '/src'  // @ 别名指向 src 目录
    }
  }
})
```

### `tailwind.config.js`

```javascript
export default {
  content: [
    "./index.html",
    "./src/**/*.{vue,js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

---

## 📈 性能优化

### 已实现的优化

- ✅ **路由懒加载** - 按需加载页面组件
- ✅ **Computed 缓存** - 避免重复计算
- ✅ **分页加载** - 减少单次渲染数量
- ✅ **图片懒加载** - 延迟加载图片资源
- ✅ **Tree Shaking** - Vite 自动移除未使用代码

### 推荐的优化

- 🔄 **虚拟滚动** - 处理超长列表
- 🔄 **CDN 加速** - 静态资源使用 CDN
- 🔄 **PWA 支持** - 离线可用
- 🔄 **代码分割** - 进一步减小包体积

---

## 🚀 部署指南

### 构建生产版本

```bash
npm run build
```

生成的文件在 `dist/` 目录下。

### 部署到 Nginx

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理
    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 部署到 Vercel/Netlify

```json
// vercel.json
{
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
```

---

## 📦 依赖说明

### 生产依赖

| 依赖 | 版本 | 说明 |
|------|------|------|
| vue | ^3.4.21 | 核心框架 |
| vue-router | ^4.3.0 | 路由管理 |
| pinia | ^2.1.7 | 状态管理 |
| axios | ^1.6.8 | HTTP 客户端 |

### 开发依赖

| 依赖 | 版本 | 说明 |
|------|------|------|
| vite | ^5.2.8 | 构建工具 |
| @vitejs/plugin-vue | ^5.0.0 | Vue 插件 |
| tailwindcss | ^3.4.3 | CSS 框架 |
| postcss | ^8.4.38 | CSS 处理 |
| autoprefixer | ^10.4.19 | 浏览器前缀 |

---

## 🤝 贡献指南

### 开发流程

1. Fork 项目
2. 创建特性分支（`git checkout -b feature/AmazingFeature`）
3. 提交更改（`git commit -m 'Add some AmazingFeature'`）
4. 推送到分支（`git push origin feature/AmazingFeature`）
5. 创建 Pull Request

### 提交规范

```
feat: 新功能
fix: 修复问题
docs: 文档更新
style: 代码格式
refactor: 重构
test: 测试相关
chore: 构建/工具
```

---

## 📄 项目文档

- [前后端联调指南](./前后端联调指南.md)
- [登录按钮优化](./登录按钮优化.md)
- [注册流程优化](./注册流程优化.md)
- [个人中心优化](./个人中心优化.md)
- [移除游客模式](./移除游客模式完成.md)
- [系统启动显示登录按钮](./系统启动显示登录按钮.md)
- [评论同步到个人中心](./评论同步到个人中心.md)
- [推荐书籍页面开发](./推荐书籍页面开发.md)

---

## 🔗 相关链接

- [Vue 3 文档](https://cn.vuejs.org/)
- [Vite 文档](https://cn.vitejs.dev/)
- [Vue Router 文档](https://router.vuejs.org/zh/)
- [Pinia 文档](https://pinia.vuejs.org/zh/)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [Axios 文档](https://axios-http.com/zh/)

---

## 📞 联系方式

如有问题或建议，欢迎通过以下方式联系：

- **Issue**: [GitHub Issues](https://github.com/your-repo/issues)
- **Email**: your-email@example.com

---

## 📜 开源协议

本项目采用 [MIT](LICENSE) 协议开源。

---

## 🙏 致谢

感谢以下开源项目：

- [Vue.js](https://vuejs.org/) - 渐进式 JavaScript 框架
- [Vite](https://vitejs.dev/) - 下一代前端构建工具
- [Tailwind CSS](https://tailwindcss.com/) - 实用优先的 CSS 框架
- [Font Awesome](https://fontawesome.com/) - 图标库

---

**⭐ 如果这个项目对你有帮助，请给一个 Star！**

---

## 📅 更新日志

### v0.1.0 (2025-12-01)

- ✅ 完整的用户认证系统
- ✅ 首页、热门书籍、推荐书籍页面
- ✅ 个人中心与书架管理
- ✅ 评论互动功能
- ✅ 响应式设计
- ✅ 移除游客模式，强制登录
- ✅ 推荐书籍独立页面
- ✅ 评论同步到个人中心

---

**🎉 BookHub - 让阅读更美好！**